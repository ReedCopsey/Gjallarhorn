<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SimpleDataEntry.Page1">
  <StackLayout>
    <Grid x:Name="Top">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Label Grid.Column="0" Text="{Binding Current, StringFormat='Current Value = {0:N0}'}" />
      <Button Grid.Column="1" Text="Increment" Command="{Binding Increment}"/>
      <ActivityIndicator Grid.Row="1" Grid.Column="0" Color="Green" IsRunning="{Binding IsValid}" />    
    </Grid>
    <Frame OutlineColor="Silver">
      <Grid x:Name="Bottom">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Entry  Grid.Row="0" Grid.Column="0" Text="{Binding Current}" Placeholder="Enter Value..." />
        <Button Grid.Row="0" Grid.Column="1" Text="Decrement" Command="{Binding Decrement}"/>
        
        <Label  Grid.Row="1" Grid.Column="0" Text="{Binding Current-IsValid, StringFormat='Valid?: {0}'}" />
        <Label  Grid.Row="1" Grid.Column="1" Text="{Binding Current-Errors[0]}" TextColor="Red" />
    
        <Label  Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding CurrentValue, StringFormat='Mutable Value: {0}'}" TextColor="Green" />
      </Grid>
    </Frame>
  </StackLayout>
</ContentPage>